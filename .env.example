# Copy this file to .env.local and paste your real keys there.
# .env.local is gitignored and never reaches the client bundle.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: live traffic evidence (free key at developer.tomtom.com).
# Without it, the traffic row simply doesn't appear.
TOMTOM_API_KEY=your-tomtom-key-here
